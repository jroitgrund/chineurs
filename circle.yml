machine:
  python:
    version: 3.5.2
dependencies:
  post:
    - npm install:
      cd chineurs/frontend;npm install

test:
  override:
    - make lint
    - make test
deployment:
  develop:
    branch: develop
    commands:
      - ssh jonathan@roitgrund.me '/home/jonathan/chineurs-dev/deploy.sh'
