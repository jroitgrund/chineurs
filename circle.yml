machine:
  python:
    version: 3.5.2
test:
  override:
    - make lint
    - make test
deployment:
  develop:
    branch: develop
    commands:
      - ssh jonathan@roitgrund.me '/home/jonathan/chineurs-dev/deploy.sh'
      - ssh jonathan@roitgrund.me 'sudo systemctl restart chineurs-dev.service'
